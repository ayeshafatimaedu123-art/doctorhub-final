# Development — Vite proxy handles /api -> localhost:5000
# No VITE_API_URL needed for local dev

# Production (Vercel) — set this in Vercel dashboard
VITE_API_URL=https://your-render-app.onrender.com
